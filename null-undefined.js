
// null vs undefined

/* undefined
>> variable value not assigned
>> function but didn't write return 
>> wrote return but didn't return anything
>> parameter that isn't passed
>> property that dosen't exists in a object

*/

let First;
function second(x, y){
    const sum = x + y;
    return;
    return fun = a +blur;
    return sum;
}
const result = second(3, 91);
console.log(result);